{
  "name": "billiard-worker",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker + Durable Objects: lobby signaling, authoritative game rooms, signed-result stats for billiard.eth",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "viem": "^2.21.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240909.0",
    "typescript": "^5.6.0",
    "wrangler": "^3.78.0"
  }
}
